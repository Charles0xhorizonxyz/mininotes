const {chromium}=require('@playwright/test'),assert=require('node:assert/strict'),fs=require('node:fs');
(async()=>{const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5173');await page.getByRole('button',{name:'Write your first note'}).click();await page.getByLabel('Note title',{exact:true}).fill('First thought');await page.getByLabel('Note content',{exact:true}).fill("A quiet place to think. 🌱\nCharles's notebook.");await page.getByRole('button',{name:'Close note'}).click();await page.reload();await page.getByRole('heading',{name:'First thought'}).waitFor();await page.getByRole('heading',{name:'First thought'}).click();await page.getByRole('button',{name:'☆ Pin',exact:true}).click();await page.getByRole('button',{name:'Move to trash'}).click();assert.equal(await page.getByRole('heading',{name:'First thought'}).count(),0);await page.locator('[data-view="trash"]').click();await page.getByRole('heading',{name:'First thought'}).click();await page.getByRole('button',{name:'Restore note'}).click();await page.locator('[data-view="all"]').click();await page.getByLabel('Search notes').fill('no match');assert.equal(await page.locator('.note-card').count(),0);await page.getByLabel('Search notes').fill('');
// Screenshot fixture is confined to this disposable browser context.
await page.evaluate(()=>{const titles=['Small things worth keeping','A slower kind of Sunday','Ideas for a rainy afternoon','Books to come back to','Somewhere along the coast'];const bodies=['The light through the kitchen window. A conversation that went a little longer than expected.\n\nRemember to notice.','Coffee before the world wakes up. A long walk with no particular destination. Make room for a little less.','A tiny garden on the balcony. A notebook of recipes from friends. Something made by hand.','The kind of stories that leave the world feeling a little bigger when you close the last page.','Find a place by the water. Bring a book, leave the schedule. Watch the afternoon go by.'];const all=JSON.parse(localStorage.getItem('mininotes.v1'));titles.forEach((title,i)=>all.push({id:'fixture'+i,title,body:bodies[i],tag:['Journal','Personal','Ideas','Reading','Travel'][i],pinned:i===0,deleted:false,updated:Date.now()-i*86400000}));localStorage.setItem('mininotes.v1',JSON.stringify(all));});await page.reload();await page.getByRole('heading',{name:'Somewhere along the coast'}).waitFor();fs.mkdirSync('test-results',{recursive:true});await page.screenshot({path:'test-results/desktop.png',fullPage:true});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'test-results/mobile.png',fullPage:true});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);assert.deepEqual(errors,[]);await browser.close();console.log('UI checks passed: create, autosave, reload, pin, trash, restore, search, responsive layout.');})().catch(e=>{console.error(e);process.exitCode=1;});
